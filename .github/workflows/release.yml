name: Release Build

on:
  push:
    tags:
      - 'v*.*.*'  # 触发条件: 推送 v0.1.1 等版本 tag
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write  # 允许创建 Release


jobs:
  build-and-release:
    runs-on: windows-2022  # 使用 VS 2022 环境
    strategy:
      matrix:
        arch: [x64, Win32]  # Win32 = x86
        include:
          - arch: x64
            arch_name: x64
          - arch: Win32
            arch_name: x86
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup MSVC
      uses: microsoft/setup-msbuild@v2
    
    - name: Configure CMake (${{ matrix.arch_name }})
      run: |
        cmake -B build-${{ matrix.arch_name }} -S . -DCMAKE_BUILD_TYPE=Release -A ${{ matrix.arch }}
    
    - name: Build Release (${{ matrix.arch_name }})
      run: |
        cmake --build build-${{ matrix.arch_name }} --config Release
    
    - name: Install to Distribution Directory (${{ matrix.arch_name }})
      run: |
        cmake --install build-${{ matrix.arch_name }} --prefix ./dist/NeteaseHookSDK-${{ github.ref_name }} --config Release
    
    - name: Upload Artifacts (${{ matrix.arch_name }})
      uses: actions/upload-artifact@v4
      with:
        name: bins-${{ matrix.arch_name }}
        path: dist/
        retention-days: 1

  package-release:
    needs: build-and-release
    runs-on: windows-2022
    steps:
    - name: Download All Artifacts
      uses: actions/download-artifact@v4
      with:
        path: dist/
    
    - name: Merge Architectures
      run: |
        New-Item -ItemType Directory -Force -Path merged
        # 使用 -Force 强制合并文件夹，解决 "already exists" 错误
        Copy-Item -Recurse -Force -Path dist/bins-x86/* -Destination merged/
        Copy-Item -Recurse -Force -Path dist/bins-x64/* -Destination merged/
    
    - name: Package Release
      run: |
        cd merged
        Compress-Archive -Path NeteaseHookSDK-${{ github.ref_name }} -DestinationPath ../NeteaseHookSDK-${{ github.ref_name }}-windows-multiarch.zip
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          NeteaseHookSDK-${{ github.ref_name }}-windows-multiarch.zip
        generate_release_notes: true  # 自动生成变更日志 (基于 PR/Commit)
        body: |
          ## 🚀 NeteaseHookSDK ${{ github.ref_name }} (Stable Release Candidate)
          
          本项目是针对网易云音乐 (Netease Cloud Music) 桌面客户端的进程度监控与数据捕获解决方案。v0.1.2 引入了深度的运行时日志控制与稳定性防护。
          
          ### 🛠 v0.1.2 日志控制与稳定性 (Logging & Stability)
          - **全运行时控制**: 新增 `--verbose`, `--silent`, `--log=file` 参数，支持动态开启/重定向日志。
          - **静默优先**: SDK 默认进入静默模式，不再干扰宿主程序的标准输出。
          - **线程安全日志**: 重构 `SimpleLog` 子系统，引入互斥锁防止并发日志乱序。
          - **启动安全防护**: 在 WIN32 模式下增加初始化失败反馈，确保无窗口模式下的异常反馈。
          
          ### 🏗 核心突破 (RE Highlights)
          - **非侵入式驻留**: 利用 Windows PE 加载特性实现 `version.dll` 劫持，完美避开 V8 引擎底层指针压缩导致的内存扫描失效。
          - **CDP 桥接技术**: 动态注入 JS Payload 至渲染层，实时捕获 `audioplayer.onPlayProgress` 事件。
          
          ### ✨ WebAPI & 增强功能
          - **智能歌词系统**: 实现 Cache-Aside 缓存策略，兼容官方加密格式，支持 LRC/翻译/罗马音同步。
          - **音频风暴可视化**: 集成 WASAPI 环形捕捉，实时 FFT 频谱分析。
          
          ### 🎨 Demo 应用更新 (NeteaseMonitor)
          - **沉浸式黑胶渲染**: 经过微调的 **1.2 RPM** 极慢旋转感，完美还原网易云沉浸式播放体验。
          - **可视化进阶**: 引入流体丝线与粒子系统，支持极光背景与玻璃拟态 Shader 渲染。
          
          ### 📦 Release 包使用说明
          
          **快速上手**：
          1. 解压 zip 文件
          2. 进入 `bin/x64` (或 `bin/x86`) 目录
          3. 直接运行 `NeteaseMonitor.exe`
          4. 首次使用按 `Ctrl + I` 安装 Hook，程序将自动重启网易云音乐
          
          **系统要求**：
          - Windows 7 / 8 / 10 / 11 (x86 或 x64)
          - [VC++ 2015-2022 Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)
          
          ### 🛠 构建修复与审计
          - 修复 `NeteaseDriver::Instance()` 符号丢失导致的 LNK2019 链接错误。
          - 解决了 x64 环境下 `AudioCapture` 回调函数的生命周期管理冲突。
          
          ---
          详细文档请参阅 [README.md](https://github.com/${{ github.repository }}/blob/${{ github.ref_name }}/README.md)。
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
