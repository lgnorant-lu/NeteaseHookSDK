# NeteaseMonitor v0.1.0 原始全量设计与修复蓝图 (Absolute Archive)

**[MODE: ARCHIVE]**
**协议**: RIPER-MCP 智能工程协议 (Dynamic Documentation)
**归档目标**: 永久保留 v0.1.0 版本的完整视觉体系、技术原案及发布前修复清单。

---

## 🎨 第一部分：沉浸式音乐可视化设计方案 (Full UI Design)

### 1. 设计哲学
**核心主题**: **"从信息监控到艺术体验"**
从当前的功能性监控工具升级为兼具实用性与美学的沉浸式音乐伴侣。设计灵感：
- **物理唱片机美学**：旋转唱片、机械质感。
- **流体动力学**：音频波形的流动、呼吸感。
- **赛博朋克氛围**：半透明、霓虹光效、动态粒子。

### 2. 专家深度洞察 (Expert Insights - The "Wow" Factor)
- **材质与物理模拟 (Materials & Physics)**:
    - **各向同性高光 (Isotropic Vinyl)**: 在唱片表面增加基于着色器的环形密集高光，模拟黑胶唱片的物理凹槽感。
    - **物理唱针 (Tonearm Logic)**: 增加动态唱针。播放开启时平滑落下；暂停时拾起。位置映射 `currentProgress / totalDuration`。
    - **各向异性模糊 (Anisotropic Blur)**: 唱片旋转重影采用各向异性模糊，增加视觉速度感。
- **"声学风暴" 可视化 (The Acoustic Storm)**: 
    - 抛弃传统的柱状频谱图。改用**多层流体丝线 (Fluid Silk Ribbons)**。
    - **低频 (Bass)**: 驱动深棕色、粗大、沉重的波动，作为底色。
    - **高频 (Highs)**: 驱动金黄色、纤细的"电火花"在丝线间脉冲。
- **动态磨砂玻璃 (Animated Frosted Glass)**: 背景产生色彩衍射 (Color Diffraction) 和边缘光晕 (Edge Glow)。

### 3. 深度创意延伸 (Phase 2 Brainstorm)

#### 🔮 视觉极致化与交互创新
| 分类 | 特性 | 描述 | 技术实现 |
|------|------|------|----------|
| **视觉** | 全息投影 | 彩虹边缘+扫描线+RGB分离 | GLSL Shader |
| **视觉** | 玻璃拟态2.0 | 毛玻璃+边缘高光+内阴影 | 高斯模糊+Alpha |
| **交互** | 物理惯性 | 窗口拖拽惯性滑动+弹性反弹 | 物理模拟 |
| **交互** | 搓碟交互 | 拖拽唱片模拟DJ搓碟 | 实时速度提取 |
| **环境** | 桌面穿透 | 背景穿透与系统渲染层融合 | `FLAG_WINDOW_TRANSPARENT` |

---

### 布局方案 (ASCII Layout)

#### 🔹 基础态 (Compact Mode) - 300x400px
```
┌────────────────────────┐
│   🎵 Song Title        │ ← 滚动文本 (Marquee)
│   👤 Artist - Album    │
├────────────────────────┤
│      ╭─────────╮       │
│      │ [Cover] │       │ ← 旋转唱片 (CSS3D 效果)
│      ╰─────────╯       │
├────────────────────────┤
│ [00:21] ━━━●─ [03:26] │ ← 进度条
├────────────────────────┤
│ 🎶 副歌：幸好...       │ ← 单行歌词
└────────────────────────┘
```

#### 🔸 扩展态 (Expanded Mode) - 800x600px
```
        ╔═══════════════ 音频可视化区 ═══════════════╗
        ║  🌊🌊🌊 动态频谱波 🌊🌊🌊                ║
        ╚═════════════════════════════════════════════╝
    ┌────────────┬────────────────────┬────────────┐
    │  封面放大  │    核心信息块        │  功能扩展  │
    │ ╭─────╮    │ │ Song: XXX      │ │  ⚙️ 设置  │
    │ │Cover│ 旋转│ │ ID: 1234567    │ │  📊 音质  │
    │ ╰─────╯    │ │ [==●======]    │ │  🎨 主题  │
    └────────────┴────────────────────┴────────────┘
        ╔═══════════════ 歌词投影区 ═══════════════╗
        ║  ▶ 幸好冬天来临在是我最想你的...  (Current)║
        ╚═════════════════════════════════════════════╝
```

---

### 4. 核心组件实现 (Implementation)

#### A. 音频可视化系统 (伪代码实现)
```cpp
struct AudioVisualizer {
    float spectrum[32];      // FFT 频谱数据
    void Update(float* audioSamples) {
        PerformFFT(audioSamples, spectrum);
        // 生成流体曲线并进行 Catmull-Rom 样条平滑
        // 在高能量区 (energy > 0.7) 发射粒子 (Additive Blend)
    }
};
```
#### B. 智能歌词同步 (五行渲染策略)
```cpp
void RenderLyrics(float currentTime) {
    int current = GetCurrentLyricIndex(lyrics, currentTime);
    for (int offset = -2; offset <= 2; offset++) {
        float scale = (offset == 0) ? 1.0f : 0.7f + 0.15f * (2 - abs(offset));
        float alpha = (offset == 0) ? 1.0f : 0.4f + 0.25f * (2 - abs(offset));
        Color color = (offset == 0) ? PULSE_ACCENT : GRAY;
        DrawTextPro(font, lyrics[idx].text, {x, y}, scale, ColorAlpha(color, alpha));
    }
}
```

---

## 🏗️ 第二部分：技术栈与风格指南 (Tech Stack & Style)

### 1. 技术栈选型
| 库名 | 用途 | 选型理由 |
|------|------|----------|
| **Raylib 5.0** | 图形渲染 | 新增 Spline API 与 GLSL 支持 |
| **stb_image** | 图片加载 | 轻量级、无外部依赖 |
| **kiss_fft** | FFT 变换 | 比 FFTW 更轻量，适合实时处理 |
| **miniaudio** | 音频采集 | 单头文件支持 WASAPI Loopback |

### 2. 视觉风格指南
- **霓虹赛博 (Default)**: Cyan (#00F5FF), Magenta (#FF00E5), Deep Blue (#0A0E27)。
- **字体**: Rajdhani (标题), Noto Sans SC (正文), Source Han Sans CN Light (歌词)。

## Ⅲ. ⚙️ v0.1.0 最终发布整合与修复计划 (Fix Roadmap)

### 1. 构建修复 (Critical Fix: LNK2019)
- **实现 `NeteaseDriver::Instance()`**: 在 `NeteaseDriver.cpp` 中定义静止局部单例，确保符号解析。
- **构造函数规范化**: 确保 `NeteaseDriver()` 正确初始化 `m_CDP`, `m_Monitoring` 等成员。

### 2. 交互体验微调 (Sensation Tuning)
- **转速优化**: 经过实测，将初始设计的 30.0 RPM 降至 **1.2 RPM**，以模拟极致缓慢、优雅的黑胶旋转感。

### 3. 发布审计 (Release Audit)
- **头文件暴露**: 确认 `NeteaseDriver.h`, `NeteaseAPI.h`, `SharedData.hpp`, `SimpleLog.h` 包含在 `include/` 发行目录。
- **包结构**: `README.md` 已准确映射 release 目录树。
- **日志标准化**: 所有模块使用 `[HH:MM:SS][LEVEL][TAG]` 格式，并由 `SimpleLog.h` 统一驱动。

---
**归档签署**: Project v0.1.0 Final Archival Team
**日期**: 2025-12-20
**状态**: [VERIFIED]
